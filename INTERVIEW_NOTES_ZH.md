# 面试笔记（中文）— 口径与追问

> 原则：每个数字都能指回 EVIDENCE_MAP；不确定就说不确定。

## 30 秒版本

「我做了一个从训练到推理的完整链路：在 8 卡 B300 上给多模态大模型
MiniCPM-o 4.5 训练投机解码的 draft 模型，接受长度从 3.49 提到 3.86；
然后把模型跨平台部署到华为 910C NPU——自己写转换和量化，压到 1.85GB
且接受率位级无损；最后在 910C 上做 profiling 驱动的优化，把实时语音
对话的 RTF 从 1.087 压到 0.4829，降了 55.6%，精度全部达标。」

## 2 分钟版本

三段讲，每段 40 秒：

**1）为什么训练（模型侧）**：推理时每个 token 都是串行解码，投机解码用
小 draft 先猜、大模型批量验证。要让 draft 猜得准，训练分布必须和推理
分布一致。我没有直接堆语料，而是用 target 模型对四个真实多模态数据源
离线生成 hidden-state cache——4197 个样本、98GiB、215 万 token，
成功率 100%，数据 sha256 绑进 manifest 保证可复核。然后 DP8 训练
5 层、block size 7 的 DSpark draft 150 步。评估用受控 A/B：逐样本
prompt 哈希多重集断言相等，保证两边做的是同一批题。结果接受长度
3.49→3.86，整体接受率 43.9%→48.5%。

**2）跨平台部署**：B300 的 checkpoint 要在 910C 上跑，得自己写转换链：
safetensors 到 GGUF 用原位换 payload（上游转换器会丢字段），量化用
混合方案——前两层走 Q8，条件主干和 markov 张量保高精度，2.26GB 压到
1.85GB。验证做到了位级：量化前后 acceptance 逐位一致。接入
llama.cpp 之后修了三个独立缺陷才把投机跑通。文本域 k=2 实测 1.87×；
但实时双工路径实测净负——RTF 反升 12%。所以我做了个明确决策：
提交版本不挂 draft。知道哪里不该用，和知道哪里该用一样重要。

**3）性能工程**：先把 RTF 分解成五段再逐段归因，否决了一堆流行假设
（malloc、lm_head、单算子换装都是零或负收益）。真正的收益三块：
自建 TileLang 后端写整段融合核，QK-norm+RoPE 融成单核 decode 吞吐
+66%，叠加 norm 融合到 +55~65%；host 税削减，每步 launch 18214→1301；
两个结构性杠杆，vision token 每帧 128→64、首 chunk 5→10 步。最终
4-run 0.4829±0.0161，相对官方基线 −55.6%，四项精度全达标。

## 可能的追问

**Q：为什么 NVIDIA 训练、Ascend 推理？**
A：训练吃吞吐和显存，B300 单机 8 卡 268GiB 适合 hidden cache 生成和
DP8；推理目标是国产 NPU 上的部署效率。这个切分本身就是跨平台系统的
典型形态：模型侧和部署侧解耦，中间用标准 artifact（GGUF + 量化）做
交接。代价是我必须自己保证两边的条件空间一致——cache 里 5 层拼接的
20480 维条件和 910C 推理时喂 draft 的特征是同构的。

**Q：DSpark 是什么？**
A：一类 EAGLE 系的投机解码 draft：不是独立小模型，而是消费 target
中间层 hidden states 的浅层网络（5 层，取 target 第 1/9/17/25/33 层），
一次提议一个 block（7 个 token）。所以它必须配对固定的 target 和层号，
换 target 立即失效。

**Q：为什么需要 hidden-state cache？**
A：两个原因：一是训练不能每步在线跑 target（太贵），离线一次生成；
二是推训一致——cache 存的就是推理时 draft 实际看到的特征分布，
这从根上消除了"训练用参考实现、推理在 NPU"的分布漂移。我还在 910C
上做过小规模本地微调来验证这件事：结论是瓶颈在数据量不在漂移，
这直接决定了后面 4197 样本的规模设计。

**Q：DP8 做了什么？**
A：纯数据并行 8 卡，local batch 1 × 8 rank × 梯度累积 4 = 全局
batch 32，no-shard（不做 ZeRO 切分）。draft 才 2.4B 参数，单卡放得下
optimizer state，DP 是最简单正确的选择。一个细节：停止条件是
max_train_steps=150 而不是 epoch——4197/32≈131 步一个 epoch，
实际训到了约 1.14 个 epoch。

**Q：profiling 如何决定优化方向？**
A：先分解后假设检验。RTF 拆成五段；每段再往下拆（decode 拆到同步
等待/算子本体，发现 48% 是 logits 同步；tts 拆到 per-token，发现 66%
是同步税）。每个候选优化都过配对 A/B，同 seed 同 harness，4-run 统计。
关键纪律是：冷热模型加载要归一化——不归一化冷启动会假扮 40% 增益。

**Q：为什么有些 kernel 优化被放弃？**
A：三类。一是实测零收益：单算子换装（RoPE、sel-embedding）——因为
瓶颈在 launch 税不在算子本身，这推翻了我自己早期的"墙不可动"结论，
后来改成整段融合就拿到 +66%。二是实测负收益：flow 的 ACL graph
capture，单段看 −20% 但 E2E +11%，回滚了。三是数值风险：某些量化
组合在 NPU 算子上有段错误。每条否决都留档，防止重蹈。

**Q：acceptance 和实际 speedup 为什么不能等价？**
A：acceptance 是"draft 猜对的比例"，speedup 还取决于 draft 的成本和
验证的摊销。k=2 时 1.87× 是因为 draft 单 token 只要 1.11ms（约
target 的 1/28）且批量验证近乎免费；但在双工场景每 chunk 只解码
5-9 个 token，draft 前向+验证摊不平，还有 chunk 间 KV 同步税——
同样的模型，换个场景就是净负。所以加速必须按场景实测，不能从
acceptance 推。

**Q（可能的诚实性追问）：哪些是团队做的？**
A：（按实际情况校准）训练管线的设计与执行、量化与转换链、910C 侧的
全部优化与 A/B 是我主导；可以补充说明工程归档在 GitHub 分支上，
每个结论都有原始日志路径可查。
